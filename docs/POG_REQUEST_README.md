# 📋 POG Request System - คู่มือการใช้งานและข้อจำกัด

## 🎯 ภาพรวม
ระบบ POG Request ใช้สำหรับให้พนักงานสาขาส่งคำขอเปลี่ยนแปลงตำแหน่งสินค้าบน Shelf (Planogram) และให้ Admin อนุมัติหรือปฏิเสธคำขอ

---

## 📱 การใช้งานฝั่ง User (สาขา)

### Action ที่ทำได้

| Action | คำอธิบาย | เงื่อนไข |
|--------|----------|----------|
| **เพิ่มสินค้า** | เพิ่ม SKU ใหม่ไปยังตำแหน่งที่ระบุ | SKU ต้อง**ไม่มี**อยู่ในสาขา |
| **ย้ายตำแหน่ง** | ย้าย SKU ไปตำแหน่งใหม่ | SKU ต้อง**มี**อยู่ในสาขาแล้ว |
| **ลบสินค้า** | ลบ SKU ออกจากตำแหน่งปัจจุบัน | SKU ต้อง**มี**อยู่ในสาขาแล้ว |

### สิ่งที่ User ทำได้
- ✅ ส่งคำขอใหม่ (pending)
- ✅ ยกเลิกคำขอที่ยังรอดำเนินการ (เปลี่ยนเป็น cancelled)
- ✅ ดูประวัติคำขอทั้งหมดของสาขาตัวเอง

---

## 👨‍💼 การใช้งานฝั่ง Admin

### Action ที่ทำได้

| Action | คำอธิบาย | ผลต่อ Stats |
|--------|----------|-------------|
| **อนุมัติ** (completed) | อนุมัติและดำเนินการ | pending -1, completed +1 |
| **ปฏิเสธ** (rejected) | ปฏิเสธพร้อมเหตุผล | pending -1, rejected +1 |
| **ลบ** (delete) | ลบรายการออกจากระบบ | status เดิม -1 |
| **Bulk Actions** | อนุมัติ/ปฏิเสธหลายรายการพร้อมกัน | อัปเดตตามจำนวน |

---

## 🛡️ การป้องกัน Bug ที่มีอยู่

### ป้องกันได้ ✅

| กรณี | การป้องกัน | รายละเอียด |
|------|------------|------------|
| **เพิ่ม SKU ซ้ำในสาขาเดียวกัน** | ปุ่ม "เพิ่มสินค้า" จะ disabled | ตรวจสอบจาก `existingLocationForBarcode` |
| **ย้าย/ลบ SKU ที่ไม่มีในสาขา** | ปุ่มจะ disabled | ต้องมี SKU ใน branch ก่อน |
| **ย้ายไปตำแหน่งเดิม** | แสดง warning และ disable ปุ่มส่ง | เปรียบเทียบ from/to position |
| **ส่งคำขอซ้ำ (Pending ค้าง)** | Backend reject พร้อม error message | เช็ค existing pending request |
| **ยกเลิกคำขอที่ไม่ใช่ pending** | Backend reject | เช็ค status ก่อน cancel |
| **Network Error** | ไม่อัปเดต local state, แสดง alert | ใช้ try-catch |
| **Backend Error** | ไม่อัปเดต local state | เช็ค `res.data?.ok` |

### ป้องกันไม่ได้ ⚠️

| กรณี | สาเหตุ | ผลกระทบ | วิธีแก้ไข |
|------|--------|---------|----------|
| **Race Condition (Admin หลายคน)** | 2 Admin เปิดหน้าพร้อมกัน | เห็นข้อมูลไม่ตรงกัน | กด **🔄 รีเฟรช** |
| **ข้อมูลเก่า (หน้าเปิดนาน)** | ไม่มี auto-refresh | Stats/Data ไม่ update | กด **🔄 รีเฟรช** |
| **ตำแหน่ง Shelf ไม่มีจริง** | ไม่มี validation กับ Shelf Master | อาจส่งคำขอที่ทำไม่ได้ | Admin ต้องตรวจสอบเอง |
| **Barcode ไม่มีใน Product Master** | Lookup อาจ fail | User ต้องพิมพ์ชื่อเอง | แสดง warning |

---

## ⚡ ประสิทธิภาพ

### Optimization ที่ทำแล้ว

| หน้า | การปรับปรุง |
|------|-------------|
| **User History Modal** | อัปเดต local state แทน API call เมื่อ cancel สำเร็จ |
| **Admin POG Requests** | อัปเดต local state + stats แทน loadData() |
| **Bulk Actions** | ไม่เรียก loadData() หลังทุก action |

### ผลลัพธ์
- ✅ ลด Network Requests
- ✅ UI อัปเดตเร็วขึ้น
- ✅ ลด Server Load

---

## 🔒 ข้อจำกัดของระบบ

### 1. ข้อจำกัดด้าน Data

| ข้อจำกัด | รายละเอียด |
|----------|------------|
| **1 SKU = 1 ตำแหน่ง/สาขา** | SKU เดียวกันไม่สามารถอยู่หลายตำแหน่งในสาขาเดียวกัน |
| **ไม่มี Swap Action** | ต้องใช้ Move แทน (อาจต้องทำ 2 ครั้ง) |
| **History ไม่มี Filter** | User เห็นทุก status รวมกัน |
| **ไม่มี Undo** | ยกเลิกได้เฉพาะ pending, หลังอนุมัติแล้วแก้ไม่ได้ |

### 2. ข้อจำกัดด้าน UX

| ข้อจำกัด | รายละเอียด |
|----------|------------|
| **Confirm Dialog ใช้ `window.confirm`** | ไม่สวย แต่ใช้งานได้ |
| **ไม่มี Real-time Update** | ต้อง refresh เพื่อเห็นข้อมูลล่าสุด |
| **ไม่มี Notification Push** | User/Admin ต้องเปิดดูเอง |

### 3. ข้อจำกัดด้าน Security

| ข้อจำกัด | รายละเอียด |
|----------|------------|
| **ไม่มี Rate Limiting** | User อาจ spam requests ได้ |
| **ไม่มี Audit Log** | ไม่บันทึกว่าใครทำอะไรเมื่อไหร่ |

---

## 📊 Status Flow

```
[User ส่งคำขอ] → pending
                    ↓
        ┌──────────┼──────────┐
        ↓          ↓          ↓
   completed   rejected   cancelled
   (อนุมัติ)   (ปฏิเสธ)   (ยกเลิก)
```

---

## 🧪 Test Cases

### User Side

| # | Test Case | Expected Result |
|---|-----------|-----------------|
| 1 | สแกน barcode ที่ไม่มีในสาขา | แสดง "เพิ่มสินค้า" เท่านั้น |
| 2 | สแกน barcode ที่มีในสาขาแล้ว | แสดง "ย้าย" และ "ลบ" (ไม่มี "เพิ่ม") |
| 3 | เลือกย้ายไปตำแหน่งเดิม | แสดง warning, ปุ่มส่งกดไม่ได้ |
| 4 | ยกเลิกคำขอ pending | สถานะเปลี่ยนเป็น "ยกเลิก" |
| 5 | ยกเลิกคำขอที่อนุมัติแล้ว | ไม่แสดงปุ่มยกเลิก |

### Admin Side

| # | Test Case | Expected Result |
|---|-----------|-----------------|
| 1 | อนุมัติ 1 รายการ | pending -1, completed +1, ไม่ reload |
| 2 | ปฏิเสธพร้อมเหตุผล | แสดงเหตุผลในหน้า UI |
| 3 | Bulk อนุมัติ 10 รายการ | Stats อัปเดตทันที |
| 4 | ลบรายการ | รายการหายจากตาราง, stats อัปเดต |
| 5 | กด refresh | โหลดข้อมูลใหม่จาก server |

---

## 📝 สรุป

**ระบบนี้เหมาะสำหรับ:**
- ✅ สาขาที่ต้องการแจ้งขอเปลี่ยน POG
- ✅ Admin ที่ต้องการจัดการหลายคำขอพร้อมกัน
- ✅ องค์กรที่มี workflow อนุมัติก่อนดำเนินการ

**ไม่เหมาะสำหรับ:**
- ❌ Real-time collaboration หลายคนพร้อมกัน
- ❌ ระบบที่ต้องการ Audit Trail ละเอียด
- ❌ การเปลี่ยนแปลง POG อัตโนมัติโดยไม่ผ่าน Admin
